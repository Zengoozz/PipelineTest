{
    "scripts": {
        "frontend:install": "cd contact && npm install",
        "frontend:build": "cd contact && npm run build",
        "backend:install": "cd server && npm install",
        "backend:build": "cd server && npm run build"
    }
}